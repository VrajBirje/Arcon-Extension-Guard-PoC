const CONFIG = {

    // Your .NET API endpoint
    API_URL:
        "http://localhost:5245/api/extensions",

    // Your local Windows service WebSocket endpoint
    SERVICE_WS_URL:
        "ws://localhost:5000/",

    // Switch extension communication to the local service
    USE_WINDOW_SERVICE:
        true,

    // Full extension scan interval
    SCAN_INTERVAL_MINUTES:
        1,

    // Debug logging
    DEBUG:
        true
};